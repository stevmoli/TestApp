<%= form_for ([@post, @post.comments.build]), method: :put do |f| %>
  <p>
    <%= f.label "Edit your comment:" %><br>
    <%= f.text_area :body %>
  </p>

  <%#= f.hidden_field :user_id, value: current_user.id %>

  <p>
    <%= f.submit %>
  </p>

<% end %>